/** @param {number[]} nums  */
function mergeSort(nums) {
	const firstLength = nums.length / 2;
	const secondLength = nums.length - Math.ceil(firstLength);
	const firstHalf = nums.slice(0, firstLength);
	const secondHalf = nums.slice(firstLength + 1);
}

function divide(nums) {

}
