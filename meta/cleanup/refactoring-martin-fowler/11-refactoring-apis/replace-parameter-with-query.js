calculateAvailableVacationTime(frank, frank.role);

function calculateAvailableVacationTime(employee, role) {
  // ...calculate available vacation time
}

// |
// v

calculateAvailableVacationTime(frank);

function calculateAvailableVacationTime(employee) {
  const role = employee.role;

  // ...calculate available vacation time
}
