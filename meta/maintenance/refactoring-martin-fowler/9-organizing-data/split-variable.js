let temp = 2 * (width + height);
console.log(temp);

temp = width * height;
console.log(temp);

// |
// v

const perimeter = 2 * (width + height);
console.log(perimeter);

const area = width * height;
console.log(area);
